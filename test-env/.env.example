# Test Environment Configuration

# Edge Gateway token (obtain from API after registering the gateway)
TEST_EDGE_TOKEN=

# API URL (default connects to main NearHome stack)
API_BASE_URL=http://api:3001

# Override number of devices via generate-compose.py
MOCK_CAMERA_COUNT=3
MOCK_BULB_COUNT=2
